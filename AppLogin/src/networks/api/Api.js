const HOST = 'https://reqres.in/';

const LOGIN_URL = 'api/login';
const GET_USER_URL = 'api/users';

// AUTHENTICATION
const LOGIN = HOST + LOGIN_URL;

// GET_LIST_USERS
const GET_USER = HOST + GET_USER_URL;

export {HOST, LOGIN, GET_USER};
